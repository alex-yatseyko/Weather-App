import React from 'react'
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
  } from 'react-native';

export const Search = ({navigation}) => {
    return (
        <View>
            <Text>Search</Text>
        </View>
    )
}
