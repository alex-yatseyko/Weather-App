import React, {useEffect} from 'react'
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
  } from 'react-native';

export const City = ({navigation}) => {
    // api.openweathermap.org/data/2.5/forecast/daily?q={city name}&cnt=7&appid={API key}

    return (
        <View>
            <Text>City</Text>
        </View>
    )
}
